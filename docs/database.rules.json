{
  "rules": {
    "rooms": {
      "TW54": {
        ".read": "auth != null",
        ".write": "auth != null",
        "players": {
          "$uid": {
            "name": {
              ".validate": "newData.isString() && newData.val().length > 0 && newData.val().length <= 20"
            }
          }
        }
      }
    },
    "werkstatt": {
      ".read": "auth != null",
      "$id": {
        ".write": "auth != null",
        ".validate": "!newData.exists() || newData.child('deleted').val() === true || (newData.hasChildren(['lf', 'frage', 'antworten', 'korrekt']) && newData.child('lf').isNumber() && newData.child('frage').isString() && newData.child('frage').val().length <= 300 && newData.child('korrekt').isNumber() && newData.child('korrekt').val() >= 0 && newData.child('korrekt').val() <= 3)"
      }
    }
  }
}
